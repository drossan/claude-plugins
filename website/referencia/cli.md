# CLI

> 🚧 **En construcción** — lo materializa la task `task-pipeline-portal-redesign-07` (issue #65): referencia de
> comandos (`claude plugin validate`, `--plugin-dir`, `/reload-plugins`, `marketplace add/install/update`) y el
> catálogo de skills.
