# Estados

> 🚧 **En construcción** — lo materializa la task `task-pipeline-portal-redesign-04` (issue #62): las máquinas
> de estado de plan y de tarea (`pending → active → completed|cancelled` y el ciclo de la tarea con
> `in_review`/`blocked`), con diagramas del tema compartido.
